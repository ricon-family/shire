#!/usr/bin/env bash
#MISE description="Resolve the identity file path for an agent"
#USAGE arg "<agent>" help="Agent name"
set -eo pipefail

SHIRE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
AGENT="$usage_agent"
IDENTITY_FILE="$SHIRE_DIR/agents/$AGENT/CLAUDE.md"

if [ -f "$IDENTITY_FILE" ]; then
  echo "$IDENTITY_FILE"
  exit 0
fi

echo "Error: no identity file for agent '$AGENT' at $IDENTITY_FILE" >&2
exit 1
